<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tvsos.mapper.TripMapper" >
    <update id="update">
        update trip
        <set>
            <if test="status != null">
                status = #{status}
            </if>
            <if test="endTime != null">
                end_time = #{endTime}
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>