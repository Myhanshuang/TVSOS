<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tvsos.mapper.TransportOrderMapper">

    <insert id="insert" parameterType="entity.TransportOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO transport_order
        (order_number, begin_poi_id, end_poi_id, est_begin_time, est_end_time, create_time, status)
        VALUES
            (#{orderNumber}, #{beginPoiId}, #{endPoiId}, #{estBeginTime}, #{estEndTime}, #{createTime}, #{status})
    </insert>

    <select id="findByStatus" resultType="entity.TransportOrder">
        SELECT * FROM transport_order WHERE status = #{status}
    </select>

    <update id="update" parameterType="entity.TransportOrder">
        UPDATE transport_order
        <set>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <select id="findById" parameterType="java.lang.Long" resultType="entity.TransportOrder">
        SELECT * FROM transport_order WHERE id = #{id}
    </select>
</mapper>